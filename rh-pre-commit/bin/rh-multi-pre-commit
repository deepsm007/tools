#!/bin/bash
set -euo pipefail

config_dir="${XDG_CONFIG_HOME:-${HOME}/.config}/pre-commit"

function try-run {
  if [[ -f "$1" ]]
  then
    pre-commit run --config $1
  else
    echo "No pre-commit config at $1"
  fi
}

try-run "${config_dir}/config.yaml"
try-run .pre-commit-config.yaml
